[project]
name = "myvenv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.1.1",
    "mcp[cli]>=1.26.0",
    "prefect>=3.6.24",
    "prefect-github>=0.4.2",
]
